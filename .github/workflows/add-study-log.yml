name: Daily Work Log Update

on:
  schedule:
    - cron: '04 14 * * *'

jobs:
  dump-value:
    name: dump-value
    runs-on: ubuntu-latest
    steps:
      - name: echo hello
        run: echo 'hello teppei'